#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Running unit tests before pushing code..."

Run the dedicated unit test script

npm run test:unit
if [ $? -ne 0 ]; then
echo "Unit tests failed. Push aborted."
exit 1
fi